<nav class="border-2">
  <div class="min-h-[40px] flex items-center justify-end">
    <% if current_user %>
      <div class="flex gap-2">
        <div> <%= link_to "Edit Account", edit_user_registration_path %></div>
        <div> <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: " mx-2" %></div>
      </div>
    <% else %>
      <div class="flex gap-2">
        <div class=""> <%= link_to "Sign Up", new_user_registration_path, class: ""  %></div>
        <div class=""> <%= link_to "Sign In", new_user_session_path, class: "mx-2"  %></div>
      </div>
    <% end %>
  </div>
</nav>